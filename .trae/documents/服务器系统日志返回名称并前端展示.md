## 后端改动
- 更新响应模型增加名称字段：在 `apps/server/src/logs/models.rs:56` 的 `LogResponse` 中新增 `application_name: Option<String>`、`instance_hostname: Option<String>`、`user_name: Option<String>`。
- 批量预取名称映射并填充：在 `apps/server/src/logs/handlers.rs:10` 的 `get_logs` 中，在分页取数后：
  - 收集 `application_id`、`instance_id`、`context.user_id` 的去重集合；
  - 通过 SeaORM 批量查询：
    - 应用：`apps/server/src/entities/applications.rs:11` 的 `name`，按 `id IN (...)` 返回映射；
    - 实例：`apps/server/src/entities/instances.rs:11` 的 `hostname`，按 `id IN (...)` 返回映射；
    - 用户：`apps/server/src/entities/users.rs:11` 的 `username`，按 `id IN (...)` 返回映射；
  - 在响应映射处（`apps/server/src/logs/handlers.rs:100-137`）为每条日志填充：
    - `application_name` 取 `app_map.get(application_id)`；
    - `instance_hostname` 取 `inst_map.get(instance_id)`；
    - `user_name` 从 `context.user_id` 提取后用 `user_map.get(user_id)`；
  - 字段为空时保持 `None`，避免前端显示 ID。
- 可选修正过滤语义：将当前“`user_id` 过滤映射到 `application_id`”的逻辑（`apps/server/src/logs/handlers.rs:27-31`、`169-171`）改为：
  - 支持 `application_id` 参数过滤 `Column::ApplicationId.eq(...)`；
  - 如需 `user_id` 过滤，则基于 `context ->> 'user_id'` 条件。

## 前端改动
- API 类型扩展：在 `apps/frontend/src/features/system/logs/data/api-schema.ts:3` 的 `logResponseSchema` 中新增可选字段 `application_name`、`instance_hostname`、`user_name`。
- System 日志表：`apps/frontend/src/features/system/logs/components/system-logs-table.tsx`
  - 应用列改用 `row.original.application_name || applicationNameMap.get(row.original.application_id) || '-'`（参考 `68-71`）；
  - 实例主机名列改为显示 `row.original.instance_hostname || '-'`（`71`）；
  - 新增“用户”列显示 `row.original.user_name || '-'`，如需保留 ID 可用悬浮提示展示。
- Request 日志表：`apps/frontend/src/features/system/logs/request/components/request-logs-table.tsx`
  - 修复应用名称列错误使用 `user_id`（`62`），改为 `row.original.application_name || applicationNameMap.get(row.original.application_id) || '-'`；
  - 实例主机名列显示 `row.original.instance_hostname || '-'`；
  - 新增“用户”列显示 `row.original.user_name || '-'`。
- 列表页面不直接显示 ID 字段，必要时通过 Tooltip/OverflowPreview 作为次要信息展示，符合“不要直接显示ID”的要求。

## 验证
- 后端：执行类型/构建检查（不运行服务），确保 `get_logs` 新增查询与模型编译通过；
- 前端：类型校验通过后，访问 `GET /api/logs?page=1&limit=20&source=server&log_type=system`，确认响应包含新增名称字段；页面显示为名称而非 ID。

## 注意事项
- 性能：名称查询使用 `IN` 批量获取，避免 N+1；仅在有对应 ID 时查询。
- 兼容：新增字段为可选，不影响旧数据；分页与过滤逻辑保持不变（除可选修正外）。
- 安全：不在日志中暴露敏感信息；沿用现有请求头脱敏逻辑。